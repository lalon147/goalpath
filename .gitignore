# Python virtual environment (created in repo root by venv)
/bin/
/include/
/lib/
/lib64
/pyvenv.cfg

# Dependencies
node_modules/

# Build output
dist/
build/
.vercel/

# Environment / secrets
.env
.env.local
.env.*.local
!.env.example

# Logs
/logs/
*.log
npm-debug.log*
yarn-error.log*

# Expo / React Native
.expo/
.expo-shared/

# Downloaded tooling
ngrok.zip

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Security assessment output — kept local. The report describes where the app
# is still soft (deferred items, accepted risks), which is not something to
# publish from a public repository.
GoalPath_Security_Assessment_*.pdf
SECURITY_ASSESSMENT_SUMMARY.md

# Coursework documents and their generator — not part of the application.
*.docx
*.pptx
Individual_reflection_*
generate_presentation.py
