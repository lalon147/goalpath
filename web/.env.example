# Base URL of the GoalPath backend API, including the /api suffix.
# Local development default is http://localhost:3000/api (used when this is unset).
# In Vercel, set this under Settings -> Environment Variables.
# The VITE_ prefix is required for Vite to expose the value to the client bundle.
VITE_API_URL=http://localhost:3000/api
